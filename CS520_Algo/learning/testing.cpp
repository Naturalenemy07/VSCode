#include <iostream>

int main() 
{
    int a = (0 + 15) / 2;
    double b = (0.0 + 15.0) / 2.0;

    std::cout<<a<<"\n";
    std::cout<<b<<"\n";

    bool c = true && false || true && true;
    std::cout<<c; 
    
}